import { ResetPasswordCreateForm } from '@widgets/auth/reset-password';

const ResetPasswordPage = () => {
  return (
    <section>
      <ResetPasswordCreateForm />
    </section>
  );
};

export default ResetPasswordPage;

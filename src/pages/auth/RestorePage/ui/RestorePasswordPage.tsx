import { RestorePasswordCreateForm } from '@widgets/auth/restore-password';

const RestorePasswordPage = () => {
  return (
    <section>
      <RestorePasswordCreateForm />
    </section>
  );
};

export default RestorePasswordPage;

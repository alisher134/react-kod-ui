import { RegisterCreateForm } from '@widgets/auth/registration';

const RegisterPage = () => {
  return (
    <section>
      <RegisterCreateForm />
    </section>
  );
};

export default RegisterPage;
